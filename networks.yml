development:
  - name: hardhat-polygon
    id: polyhard
    cmd: npx hardhat node --hostname 0.0.0.0
    host: http://127.0.0.1
    timeout: 120
    cmd_settings:
      port: 8545
      fork: polygon-test
